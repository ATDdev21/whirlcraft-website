try {
    
    // fetch('../.data/version.json').then(response => {
    //     if (!response.ok) {
    //         throw new Error("HTTP error" + response.status);
    //     }
    //     return response.json();
    // })
    // .then(json => {

    // })

} catch (error) {
    console.log(error)
}